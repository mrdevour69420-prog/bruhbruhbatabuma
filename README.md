# bruhbruhbatabum

- nhập lệnh theo thứ tự:

+ Nếu chỉ cần start sv:
=> ./start.sh

+ Dùng cái này nè: 
=> chmod +x bum.sh && ./bum.sh

* cài hệ thống
=> curl -fsSL https://packages.playit.gg/install.sh | bash

+ dùng khi muốn mở thủ công:
1. chmod +x download.sh remove.sh git.sh playit.sh   # bước đệm
2. ./download.sh -p mod.mrpack -j 67                 # tải mod
3. ./remove.sh -f remlist.txt -d ./mods              # xóa mod lỗi/không cần thiết
4. java -jar installer.jar                           # tải hệ thống
5. chmod +x start.sh                                 # bước đệm khác
6. ./start.sh                                        # chạy server

- nếu cần lưu:
=> ./git.sh